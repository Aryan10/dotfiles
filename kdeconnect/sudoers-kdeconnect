# Allow running KDE Connect firewall rule without password
# Only permits this exact command — nothing else
Nishant ALL=(root) NOPASSWD: /usr/bin/firewall-cmd --zone=nm-shared --add-service=kdeconnect
